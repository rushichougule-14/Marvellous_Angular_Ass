import { CompSuccDirective } from './comp-succ.directive';

describe('CompSuccDirective', () => {
  it('should create an instance', () => {
    const directive = new CompSuccDirective();
    expect(directive).toBeTruthy();
  });
});
